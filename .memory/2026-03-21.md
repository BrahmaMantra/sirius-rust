# 2026-03-21

## 完成

- [x] 目录结构对齐 sirius C++ 参考架构（/tmp/sirius-repo/）
  - cuda/ 只放 kernel 代码（.cu + Rust wrapper）
  - GpuContext 从 cuda/context.rs 移到 src/gpu_context.rs（对应 sirius/src/gpu_context.cpp）
- [x] 删除 CpuExecutor，只保留 MockGpuExecutor
  - 不支持 GPU 时回退 DuckDB 原生，不做 CPU fallback
  - SiriusContext::new() 默认使用 MockGpuExecutor
- [x] 实现 AVG、MIN、MAX 聚合算子
  - op/aggregate/avg.rs、min.rs、max.rs（AggregateFunction trait）
  - MockGpuExecutor 实现 avg/min/max（block parallel reduction）
  - AggregateExecutor trait 新增 avg/min/max 方法
  - 注册到 AggregateRegistry
  - 共 94 个测试全部通过
- [x] TPC-H 22 条标准查询 SQL 存入 tests/tpch/queries/q01.sql ~ q22.sql

- [x] 注册 sirius_avg、sirius_min、sirius_max、sirius_count_star 到 DuckDB
  - avg_agg.rs、min_agg.rs、max_agg.rs、count_agg.rs::update_star DuckDB C 回调
  - sirius_extension.rs 调用所有 6 个 register 函数
- [x] TPC-H benchmark 框架搭建（tests/tpch/）
  - run_native.sh：生成数据 + 跑 DuckDB 原生（多次取中位数）
  - run_sirius.sh：加载扩展 + 跑 sirius 版本查询
  - gen_sirius_queries.sh：sed 替换 agg 函数名（sum→sirius_sum 等）
  - compare.sh：并排对比 native vs sirius，计算加速比

## 关键决策

- COUNT(DISTINCT) 不实现：TPC-H 只有 Q16 用，让 DuckDB 原生处理
- sirius 聚合函数用 sirius_* 前缀注册，不替换 DuckDB 原生 sum/count 等
- 不做 CPU fallback 执行器，不支持 GPU 时直接用 DuckDB 原生
- 每条查询跑 5 次取中位数，消除 OS 调度噪音

## 待做

- [ ] Windows 笔记本测试 GPU 编译
- [ ] CudaBackend 实现（真实 GPU kernel）
- [ ] 多规模跑分（SF0.1 / SF1 / SF5 / SF10）
- [ ] Substrait 解析
- [ ] GroupBy 支持
